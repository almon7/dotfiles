[core]
	editor = nvim

# Use the personal identity everywhere. A later machine-local include can still
# override it for explicitly configured work directories.
[user]
	useConfigOnly = true

[include]
	path = ~/.gitconfig-personal

[include]
	path = ~/.gitconfig.local
